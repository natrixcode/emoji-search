import { PropsWithChildren } from 'react';
import PropTypes from 'prop-types';
declare type EmojiProviderProps = PropsWithChildren<{
    data: {
        [key: string]: string;
    };
}>;
declare function EmojiProvider({ data, children }: EmojiProviderProps): JSX.Element;
declare namespace EmojiProvider {
    var propTypes: {
        data: PropTypes.Validator<object>;
    };
}
export default EmojiProvider;
